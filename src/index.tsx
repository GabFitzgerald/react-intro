// 2 minimum packages you need for index file to work as a react app
import React from "react"
import ReactDOM from "react-dom"

ReactDOM.render(
    <h1>Hi There!</h1>,
    document.querySelector('#root')
)